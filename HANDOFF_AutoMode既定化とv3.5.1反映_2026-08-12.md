> # ⚠️ 第2版（2026-08-12 追記）— 再実行してください
>
> 統括が全10プロジェクトを機械検証した結果、**この部署には下記が残っています**。
>
> - **`alwaysThinkingEnabled: true` が残存** — 削除してください
> - **`@agent` 明示指名が0件** — PROACTIVE エージェント1体が対象です
> - 四境界は充足済み（deny 20件・ask 8件）✅
>
> ## 原因の一部は統括が渡した道具の欠陥です
>
> スキルの検査項目 P1〜P5 に **`alwaysThinkingEnabled` の検査が入っていませんでした**（統括の Source of Truth 側にはあったのに、スキルへ移植し忘れ）。**P6 として追加済み**です。
>
> ## 手順
>
> 1. **Claude Code アプリ／CLI を再起動**（スキルを更新したため、起動中のセッションには反映されていません）
> 2. この部署を開いて `/upgrade-project platform` を再実行
> 3. 検査 → レポート → **承認** → 修正
>
> 以下は初版の内容です。原則0（自部署の資産を実名列挙してから照合）は今回も必ず実行してください。

---

# 【受信用ダイジェスト】Auto Mode 既定化（8/14）と v3.5.1 反映 — posse

**発行**: 2026-08-12・統括 Clude開発
**正本（詳細はこちら）**: `/Users/ATSUSHITO_RYCE/CLAUDE/AD_シニア漫画/works/HANDOFF_AutoMode既定化とv3.5.1反映_2026-08-12.md`

---

## やること

**① 8/14 までに（期限あり）**

```
/upgrade-project platform
```

**② 8/14 以降・順次**

```
/upgrade-project all
```

スキルは 検査 → レポート → **承認** → 修正 の順で進む。承認前に勝手な編集はしない。

---

## この部署の実測（2026-08-12 統括 grep・**鵜呑みにしないこと**）

| 項目 | 値 |
|---|---|
| `permissions.deny` | 5件 |
| `permissions.ask` | **0件**（全10プロジェクト共通） |
| `PROACTIVELY` エージェント | 1体 |
| モデル | `claude-opus-5` ✅ |
| git | あり |

**リモート共有あり。§1 の観点④（外部への送信・公開）を特に確認する。** `commands/` を3本持つ唯一の部署なので、§2 の明示起動パスはスラッシュコマンド側の記述と揃えること。

---

## 変更は3つ

### 1. Auto Mode が 8/14 から既定になる（期限）

2026-08-14 から Pro/Max/Team の**新規セッション**は Auto Mode が既定になる。`deny` と明示 `ask` は**分類器より前に評価される恒久境界**なので、ここに穴があると分類器の判断に委ねられる。次の4観点を点検する。

```
□ ①削除・破壊的操作  □ ②main への force push
□ ③秘匿ファイルの読取  □ ④外部への送信・公開
```

`permissions.allow` は**検査対象外**（プロジェクト固有の判断）。`defaultMode: "auto"` は project/local settings では**無視される**ので書かない。

### 2. 常設委譲に明示起動パスを併記する（issue #80988・未検証）

Opus 5 のサーバ側注入で、明示指名のない自動委譲が抑制されるとの報告がある。`PROACTIVELY` エージェント → `@agent-name`、常設スキル → `/skill`、常設 Workflow → そのスラッシュコマンド。実測では**全部署で `@agent-` が0件**。マーカーは消さず、明示パスを**足す**。

### 3.「委譲の上限」の限定句を保護ロール実名列挙にする

**保護ロール = review / audit / gate・approval / fact-check。** 抑制対象は「**自分の出力をもう一度確かめるためだけの委譲**」だけ。常設委譲・人間の承認ゲート・機械チェック・外部実態との照合・自走サイクルの検証ステップは**規模にかかわらず実行する**。

---

## 原則0（省略禁止）

**このダイジェストは最大公約数で、この部署が何を持っているかを知らない。** 2026-07-29 に統括が配った1指示が4部署で品質ゲートを禁止した。

新基準を当てる**前に**、自部署の資産を実名で書き出す — 並列 Workflow／常設ルーティン／`PROACTIVELY` エージェント／必須成果物／停止ゲート／必須の計測報告／**独自表現の既存例外規定**（「例外A」「対象外」など。語が違うだけで実体は同じことが多い）。そのうえで1件ずつ照合する。

---

## 触ってはいけないもの

- **Turn-ownership ガード**（架空のユーザー発話に応答しない）— 正確性のルール。撤回対象ではない
- **既存の例外規定・保護対象の実名リスト** — 部署の設計が優先。統括の版が優れて見えても上書きしない
- **`permissions.allow` の中身**
- **コスト系ハードストップ**（1指示=1生成・自動リトライ禁止・残高照合など）— コスト設計であり旧世代対策ではない

削除は必ずユーザー承認を得てから。「必ず」「確認」「検証」での grep 一括削除は禁止。

---

## 完了後

自部署の `references/decision-log.md` に「決定・ユーザー意図・検証結果」を記録する。統括への個別報告は任意。
